# Voice Chat Server Configuration
PORT=8080
BRIDGE_PORT=9090
AUTH_TOKEN=change-me-app-token
BRIDGE_TOKEN=change-me-bridge-token

# TLS Settings (Let's Encrypt) - setup.sh will configure these
TLS_ENABLED=false
TLS_CERT=
TLS_KEY=
